# The OpenRouter roster for the live E2E suite, translated from fabro's
# `openrouter.toml` and checked
# against OpenRouter's live `GET /api/v1/models` listing on 2026-08-29.
# The roster also includes three models from the Venice union policy:
# grok-4.6, glm-5.3, and qwen3.8-27b. Live listing values win where fabro's
# limits or prices differ.
#
# Capability flags follow the listing's `supported_parameters` and input
# modalities. Spot checks confirmed forced tool calls on Claude Opus 5 and
# Laguna XS 2.1, structured-output request acceptance on Gemini 3.5 Flash,
# provider-reported `usage.cost`, and numeric error codes. The Chat codec
# cannot carry document or audio parts, so those inputs remain unclaimed.
#
# OpenRouter documents sticky prompt-cache routing through `session_id`.
# Lithos currently emits `prompt_cache_key` for `cache_routing`, so no row
# claims that capability. Claude rows do claim explicit cache breakpoints,
# which OpenRouter documents for its Chat Completions endpoint.

schema_version = 1

[providers.openrouter]
display_name = "OpenRouter"
adapter = "openai-compatible"
codec = "openai-chat"
base_url = "https://openrouter.ai/api/v1"
priority = 25
allow_passthrough = true
default_model = "claude-sonnet-5"

[providers.openrouter.auth]
type = "bearer"

# ---------- Anthropic via OpenRouter ----------

# The listing omits `tool_choice` for this row (the Fable 5 row has it), so it
# claims tools without forced_tool_choice and the forced-choice cells skip it.
# Verified 2026-09-01 against GET /api/v1/models.
[providers.openrouter.models."claude-fable-5.1"]
display_name = "Claude Fable 5.1 (via OpenRouter)"
api_model = "anthropic/claude-fable-5.1"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, forced_tool_choice = false, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true }

[providers.openrouter.models."claude-fable-5"]
display_name = "Claude Fable 5 (via OpenRouter)"
aliases = ["fable", "claude-fable"]
api_model = "anthropic/claude-fable-5"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true }

[providers.openrouter.models."claude-opus-5"]
display_name = "Claude Opus 5 (via OpenRouter)"
aliases = ["opus", "claude-opus"]
api_model = "anthropic/claude-opus-5"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true, sampling = true }

[providers.openrouter.models."claude-sonnet-5"]
display_name = "Claude Sonnet 5 (via OpenRouter)"
aliases = ["sonnet", "claude-sonnet"]
api_model = "anthropic/claude-sonnet-5"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true }

[providers.openrouter.models."claude-opus-4.8"]
display_name = "Claude Opus 4.8 (via OpenRouter)"
api_model = "anthropic/claude-opus-4.8"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true, sampling = true }

[providers.openrouter.models."claude-opus-4.7"]
display_name = "Claude Opus 4.7 (via OpenRouter)"
api_model = "anthropic/claude-opus-4.7"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true }

[providers.openrouter.models."claude-sonnet-4.6"]
display_name = "Claude Sonnet 4.6 (via OpenRouter)"
api_model = "anthropic/claude-sonnet-4.6"
limits = { context_tokens = 1000000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, cache_breakpoints = true, sampling = true }

[providers.openrouter.models."claude-haiku-4.5"]
display_name = "Claude Haiku 4.5 (via OpenRouter)"
aliases = ["haiku", "claude-haiku"]
api_model = "anthropic/claude-haiku-4.5"
limits = { context_tokens = 200000, max_output_tokens = 64000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, caching = true, cache_breakpoints = true, sampling = true }

# ---------- OpenAI via OpenRouter ----------
# The long-context rates start at 272k prompt tokens. The listing expresses
# this as `min_prompt_tokens = 272000`; Lithos selects the tier above that
# input count.

[providers.openrouter.models."gpt-5.6-sol"]
display_name = "GPT-5.6 Sol (via OpenRouter)"
aliases = ["sol", "gpt-sol", "gpt56-sol", "gpt-56-sol", "gpt-5.6", "gpt56", "gpt-56"]
api_model = "openai/gpt-5.6-sol"
limits = { context_tokens = 1050000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true }

[providers.openrouter.models."gpt-5.6-terra"]
display_name = "GPT-5.6 Terra (via OpenRouter)"
aliases = ["terra", "gpt-terra", "gpt56-terra", "gpt-56-terra"]
api_model = "openai/gpt-5.6-terra"
limits = { context_tokens = 1050000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true }

[providers.openrouter.models."gpt-5.6-luna"]
display_name = "GPT-5.6 Luna (via OpenRouter)"
aliases = ["luna", "gpt-luna", "gpt56-luna", "gpt-56-luna"]
api_model = "openai/gpt-5.6-luna"
limits = { context_tokens = 1050000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true }

[providers.openrouter.models."gpt-5.4"]
display_name = "GPT-5.4 (via OpenRouter)"
aliases = ["gpt54", "gpt-54", "gpt-5.2", "gpt5", "gpt-5.3-codex", "codex"]
api_model = "openai/gpt-5.4"
limits = { context_tokens = 1050000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true }

[providers.openrouter.models."gpt-5.5"]
display_name = "GPT-5.5 (via OpenRouter)"
aliases = ["gpt55", "gpt-55"]
api_model = "openai/gpt-5.5"
limits = { context_tokens = 1050000, max_output_tokens = 128000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true }

# ---------- Google Gemini via OpenRouter ----------

[providers.openrouter.models."gemini-3.1-pro-preview"]
display_name = "Gemini 3.1 Pro Preview (via OpenRouter)"
api_model = "google/gemini-3.1-pro-preview"
limits = { context_tokens = 1048576, max_output_tokens = 65536 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."gemini-3.5-flash"]
display_name = "Gemini 3.5 Flash (via OpenRouter)"
api_model = "google/gemini-3.5-flash"
limits = { context_tokens = 1048576, max_output_tokens = 65536 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

# ---------- Open-weights models ----------

[providers.openrouter.models."mimo-v2.5-pro"]
display_name = "Xiaomi MiMo v2.5 Pro"
api_model = "xiaomi/mimo-v2.5-pro"
limits = { context_tokens = 1050000, max_output_tokens = 131072 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."minimax-m2.7"]
display_name = "MiniMax M2.7"
api_model = "minimax/minimax-m2.7"
limits = { context_tokens = 204800, max_output_tokens = 131072 }
# OpenRouter accepted JSON-object output, but its DeepInfra route rejected
# JSON Schema on 2026-08-29; do not claim the combined capability.
capabilities = { text = true, tools = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."deepseek-v4-pro"]
display_name = "DeepSeek V4 Pro"
api_model = "deepseek/deepseek-v4-pro"
limits = { context_tokens = 1048576, max_output_tokens = 384000 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."deepseek-v4-flash"]
display_name = "DeepSeek V4 Flash"
aliases = ["deepseek-v4", "deepseek", "deepseek-flash"]
api_model = "deepseek/deepseek-v4-flash-0731"
limits = { context_tokens = 1310720, max_output_tokens = 943718 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."kimi-k3"]
display_name = "Kimi K3 (via OpenRouter)"
api_model = "moonshotai/kimi-k3"
limits = { context_tokens = 1048576, max_output_tokens = 943718 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."laguna-s-2.1"]
display_name = "Laguna S 2.1 (via OpenRouter)"
api_model = "poolside/laguna-s-2.1"
limits = { context_tokens = 1048576, max_output_tokens = 131072 }
capabilities = { text = true, tools = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."laguna-xs-2.1"]
display_name = "Laguna XS 2.1 (via OpenRouter)"
api_model = "poolside/laguna-xs-2.1"
limits = { context_tokens = 262144, max_output_tokens = 32768 }
capabilities = { text = true, tools = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."qwen3-coder"]
display_name = "Qwen3 Coder"
api_model = "qwen/qwen3-coder"
limits = { context_tokens = 262144, max_output_tokens = 65536 }
capabilities = { text = true, tools = true, structured_output = true, sampling = true }

[providers.openrouter.models."qwen3.6-flash"]
display_name = "Qwen3.6 Flash"
api_model = "qwen/qwen3.6-flash"
limits = { context_tokens = 1000000, max_output_tokens = 65536 }
# Structured output: the live listing claims `structured_outputs`, but the
# Alibaba route ignored the supplied schema twice on 2026-08-29.
capabilities = { text = true, images = true, tools = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."qwen3.8-max"]
display_name = "Qwen3.8 Max"
api_model = "qwen/qwen3.8-max"
limits = { context_tokens = 1000000, max_output_tokens = 131072 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."glm-5.2"]
display_name = "GLM 5.2 (via OpenRouter)"
aliases = ["glm", "glm5", "glm52", "glm5.2"]
api_model = "z-ai/glm-5.2"
limits = { context_tokens = 1048576, max_output_tokens = 262144 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."glm-4.6"]
display_name = "GLM 4.6"
api_model = "z-ai/glm-4.6"
limits = { context_tokens = 204800, max_output_tokens = 16384 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, caching = true, sampling = true }

[providers.openrouter.models."devstral-2512"]
display_name = "Devstral 2512"
api_model = "mistralai/devstral-2512"
limits = { context_tokens = 262144, max_output_tokens = 209715 }
capabilities = { text = true, tools = true, structured_output = true, caching = true, sampling = true }

# ---------- Venice roster union ----------

[providers.openrouter.models."grok-4.6"]
display_name = "Grok 4.6 (via OpenRouter)"
aliases = ["grok", "grok46", "grok-46"]
api_model = "x-ai/grok-4.6"
limits = { context_tokens = 500000, max_output_tokens = 450000 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."glm-5.3"]
display_name = "GLM 5.3 (via OpenRouter)"
aliases = ["glm53", "glm5.3", "glm-5-3"]
api_model = "z-ai/glm-5.3"
limits = { context_tokens = 1310720, max_output_tokens = 131072 }
capabilities = { text = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }

[providers.openrouter.models."qwen3.8-27b"]
display_name = "Qwen 3.8 27B (via OpenRouter)"
aliases = ["qwen-27b", "qwen-3.8-27b", "qwen38-27b"]
api_model = "qwen/qwen3.8-27b"
limits = { context_tokens = 1000000, max_output_tokens = 131072 }
capabilities = { text = true, images = true, tools = true, structured_output = true, reasoning = true, reasoning_effort_levels = true, caching = true, sampling = true }
