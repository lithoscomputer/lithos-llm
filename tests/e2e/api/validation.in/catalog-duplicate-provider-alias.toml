schema_version = 1

[providers.first]
display_name = "First"
aliases = ["shared"]
adapter = "custom"
codecs = ["custom"]
base_url = "https://example.com"
auth = { type = "none" }

[providers.second]
display_name = "Second"
aliases = ["shared"]
adapter = "custom"
codecs = ["custom"]
base_url = "https://example.com"
auth = { type = "none" }
